<div class="maincontent-create">
<div id="middle-mw-top">
<div id="top-bg-new">&nbsp;</div>
<div style="width:995px; clear:both;">
<h1 class="plain">Notify Your Invitees</h1>

<p class="bold">You have successfully updated your invite "<%= @invitation.name %>".  
Please select the Invitees you would like us to notify (recommended if the time or place of the proposed meeting is changed) from the list below.
</p>

<p class="selectit">Select the Invitees you'd like to notify of your changes:</p>

<% form_tag(notify_after_edit_url(:id => @invitation.id)) do -%>
  <div class="middle">

    <ul>
      <% @invitees.each do | invitee | %>
        <% if invitee.is_a?(Member) %>
          <li>
          <%= check_box_tag("members[]", invitee.id, true) %>  
          <%= invitee.user_name %>
          <%= "(#{invitee.fullname})" unless invitee.fullname.blank? %>
          </li>
        <% elsif invitee.is_a?(NonMember)  %>
          <li> 
          <%= check_box_tag("non_members[]", invitee.id, true) %>  
          <%= invitee.email %>
          </li>
        <% end %>

      <% end %>
    </ul>

  </div>

  <div id="send-notifications-buttons">
    <%= button_image_delete("send_notifications") %>
    <%= link_to("Continue without sending notifications >", show_invite_url(:id => @invitation.id)) %>
  </div>


<% end %>
<br/>
<br/>
<br/>
</div>
       	<div id="sub-bottom-mw">&nbsp;</div>
        <div id="bottom-mw">&nbsp;</div>      


</div>
</div>